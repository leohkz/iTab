/**
 * usePageAnimations — stub
 * GSAP feature removed. This hook is a no-op.
 */
import { useEffect } from 'react';

export function usePageAnimations(_enabled: boolean) {
  useEffect(() => {
    // no-op
  }, [_enabled]);
}
